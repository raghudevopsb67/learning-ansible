- name: Demo on Loops
  hosts: localhost
  tasks:
    - name: Print Debug
      ansible.builtin.debug:
        msg: Fruit Name = {{item}}
      loop:
        - apple
        - banana
        - orange
